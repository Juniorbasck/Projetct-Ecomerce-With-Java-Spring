'use strict';

describe('myApp.manege module', function() {

  beforeEach(module('myApp.manege'));

  describe('manege controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var Manegetrl = $controller('Manegetrl');
      expect(Manegetrl).toBeDefined();
    }));
  });
});